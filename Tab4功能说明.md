# Tab4 规则管理器 - 功能说明

## 概述
Tab4 规则管理器是一个可视化的配置管理界面，用于管理系统的解析规则配置，包括男主映射、密探映射、以及各类分类映射。

## 主要功能

### 1. 规则类型管理
支持以下 5 种规则类型：
- **男主映射**：拼音缩写 → 男主中文名（如：lb → 刘辩）
- **密探名映射**：拼音 → 密探中文名（如：sunfu → 孙辅）
- **户外分类**：代码 → 户外分类名
- **户内分类**：代码 → 户内分类名
- **地板分类**：代码 → 地板分类名

### 2. CRUD 操作
- **新增**：点击"新增"按钮，添加新的键值对映射
- **编辑**：点击编辑按钮，修改已有的映射关系
- **删除**：点击删除按钮，删除指定映射（带二次确认）
- **搜索**：实时搜索键或值，快速定位条目

### 3. 数据导入导出
- **导出规则**：将当前所有规则导出为 JSON 文件
- **导入规则**：从 JSON 文件导入规则配置
- **重置规则**：恢复到默认的 parseConfig.json 配置

### 4. 持久化存储
- 所有修改自动保存到 localStorage（键名：`customRulesData`）
- 应用重启后自动恢复上次的自定义规则
- 修改规则后会自动触发 `reloadRules()`，立即生效

### 5. 与其他 Tab 的集成
- **Tab1 入库功能**：
  - 点击"重新解析"按钮，会使用最新的自定义规则重新解析所有图片
  - 上传新图片时，自动使用当前生效的规则进行文件名解析
  
## 使用流程

### 修改现有规则
1. 切换到对应的规则类型标签（如"男主映射"）
2. 找到要修改的条目，点击编辑按钮
3. 修改键或值，按回车或点击保存
4. 返回 Tab1，点击"重新解析"按钮，查看效果

### 添加新规则
1. 选择要添加规则的类型标签
2. 点击右上角"新增"按钮
3. 填写键和值，保存
4. 新规则立即生效

### 批量导入规则
1. 准备好符合格式的 JSON 文件（参考导出的文件格式）
2. 点击"导入"按钮，选择文件
3. 导入成功后，所有规则更新为导入的配置

## JSON 文件格式
```json
{
  "男主映射": {
    "lb": "刘辩",
    "fr": "傅融"
  },
  "密探名映射": {
    "sunfu": "孙辅"
  },
  "户外分类": {
    "outdoor": "户外"
  },
  "户内分类": {
    "indoor": "户内"
  },
  "地板分类": {
    "diban": "地板"
  }
}
```

## 技术细节

### 存储结构
localStorage 中存储的格式（Array 格式）：
```json
{
  "maleLeads": [{"key": "lb", "value": "刘辩"}],
  "spyNames": [{"key": "sunfu", "value": "孙辅"}],
  "outdoorCats": [...],
  "indoorCats": [...],
  "floorCats": [...]
}
```

### 规则加载优先级
1. 优先从 localStorage 读取用户自定义规则
2. 如果没有自定义规则，使用默认的 `parseConfig.json`
3. 每次修改规则后，自动调用 `reloadRules()` 通知 types.ts 重新加载

### 与 Tab1 的联动
- Tab1 中的文件名解析使用 `types.ts` 中的 `parseFileName()` 函数
- `parseFileName()` 函数会自动使用当前生效的规则（通过 `loadRules()` 动态加载）
- 修改 Tab4 的规则后，点击 Tab1 的"重新解析"按钮即可应用新规则

## 注意事项
1. **键名唯一性**：同一类型下的键不能重复，系统会自动检测并提示
2. **立即生效**：规则修改后立即保存到 localStorage 并触发 `reloadRules()`
3. **备份重要**：建议在大量修改前，先导出当前规则作为备份
4. **重置不可恢复**：点击"重置"后，所有自定义规则将被清空，无法撤销
